#!/bin/bash
# Post-merge setup for Bounds Android project.
# Android builds happen in Android Studio / CI — nothing to install here.
set -e
echo "Post-merge setup complete (Android project — no server-side steps required)"
