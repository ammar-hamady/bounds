<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Bounds" parent="android:Theme.Material.Light.NoActionBar"/>

    <!-- Theme for BlockedOverlayActivity: no action bar, dark translucent window -->
    <style name="Theme.Bounds.BlockedOverlay" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>